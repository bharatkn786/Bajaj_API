Note: The service is deployed on Render free tier, so the first request may take a few seconds due to cold start.
Available APIs: GET /health and POST /bfhl (with request body and accepts JSON body).
